# Nenhum segredo é necessário para o build.
# Se um dia precisar, o valor real vive nas variaveis de ambiente do
# Cloudflare Pages — NUNCA neste arquivo, NUNCA no .env commitado.

# Exemplo (ainda nao usado):
# HUBSPOT_PORTAL_ID=00000000     # publico, vai no HTML do form embed
