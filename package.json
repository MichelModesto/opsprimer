{
  "name": "opsprimer",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "setup": "git config core.hooksPath .githooks && echo 'hooks ativados'",
    "check:secrets": "gitleaks detect --source . --verbose"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.2"
  }
}
